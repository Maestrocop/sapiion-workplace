# Leave unset for normal dev/self-host use — requests go through Vite's
# dev proxy (or nginx in production) on whatever origin/port the frontend
# actually runs on. Only set this if the frontend and backend are served
# from genuinely different domains with no proxy in between.
# VITE_API_URL=http://localhost:4100
